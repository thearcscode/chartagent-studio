# Architecture decisions

Studio's own decisions live here, numbered from `0001`. They are cited as
**Studio ADR-000N**; the library's are cited as **ADR-000N** and live in
`docs/adr/` of [`thearcscode/chartagent`](https://github.com/thearcscode/chartagent).
